console.log(Boolean(239 > 1912)); // evalute the expression
console.log(Boolean("false")); // any value in the function evaluates true
console.log(Boolean(0)); // 0 is the binary false so returns false
let x;
console.log(Boolean(x)); // x is undefined...returns false

x = null;
console.log(Boolean(x)); // null returns false
